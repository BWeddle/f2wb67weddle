extends layout

block content
  h1= title
  - var results = ['Catan', 'Cards Against Humanity', 'The Legend of Zelda']
  div(class="container-fluid text-center")
    div(class="row")
      each item in results
        div(class="col-sm-2") #{item}
      